const router = require ('express').Router()
const { postUserController } = require('./../controllers/users')

router.get('/', async (req, res) => {
    ............copio lo que tengo en el index
})


module.exports = router